#include <stdio.h>
int main()
{
    int i, j, a, t, n;
    scanf("%i%i", &t, &n);
    const int T = t;
    const int N = n;
    int cidades[N];
    getchar();
    for(i=0; i<N; i++)
    {
        scanf("%i", &cidades[i]);
    }
    
    

    return 0;
}